.topwrapper
  .background-signout
    .explanationbox
      .top-title
        NomadCafe
        %br
      .explanation
        ノマドワークに適した、wifi・電源フリーのお店をすぐにご案内します。


      .user-sessions-form
        %span.title サ イ ン イ ン
        .blankspace
        = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
          -# = render "devise/shared/error_messages", resource: resource
          .field
            = f.label :ニックネーム
            %br/
            = f.text_field :nickname, class:'nickname', placeholder:"入力不要です", autofocus: true
          .field
            = f.label :パスワード
            - if @minimum_password_length
              %em
                (#{@minimum_password_length} 文字以上)
            %br/
            = f.password_field :password, class:'password', placeholder:"入力不要です",  autocomplete: "new-password"
          .field
            = f.label :パスワード（再確認）
            %br/
            = f.password_field :password_confirmation, class:'password_confirmation', placeholder:"入力不要です", autocomplete: "new-password"
          .actions
            = f.submit "サインアップ",class:"login-btn"

      .easy-login
      = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
        = f.hidden_field :nickname, value: "TEST"
        = f.hidden_field :password, value: "test0000"
        = f.hidden_field :password_confirmation, value: "test0000"
        = f.submit "かんたんログイン", class:"e-login-btn"